Permutation tests

Let's start by assuming that circuits downstream from the visual cortex can't decode whether the image observed by the mouse was animate or inanimate. That
would imply that the probability to decode the animate-inanimate stimulus
from neural activity should have maximum uncertainty. The decoded label
should be Bernoulli with probability 0.5 under the null. 

--> This is wrong, but think about this. 

real semantic axis exists 
⇒
⇒ margins are stably positive across stimuli,

Under the null there is no privileged projection direction in the data. 
The points are well-mixed. 